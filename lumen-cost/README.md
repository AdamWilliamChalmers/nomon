# Lumen Cost — prompt savings coach

Paste a prompt. See cost, waste, and ranked cuts — client-side, no login.

```bash
cd lumen-cost
npm install
npm run dev    # http://localhost:3001
npm test
```

Phase 1 of `TOKENPLAN.md`: tiktoken counter, model cost table, rule-based savings engine.
